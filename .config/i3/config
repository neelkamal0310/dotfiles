# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font FiraCode:monospace 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.
exec --no-startup-id nm-applet --indicator

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1% && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1% && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal

#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec alacritty

bindsym $mod+Shift+s sticky toggle
bindsym $mod+b exec firefox
bindsym $mod+Shift+b exec brave

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run.sh
bindsym $mod+Shift+d exec $HOME/bin/dmenu_popup
# bindsym $mod+d exec --no-startup-id rofi -show run  
# bindsym $mod+Tab exec --no-startup-id rofi -show window -lines 5 -eh 2 -width 1 -padding 0 -font "Droid Sans Book 20" -theme "~/.config/rofi/window_switcher.rasi"
bindsym $mod+Tab exec --no-startup-id $HOME/bin/dmenu-i3-window-jumper
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
#bindsym $mod+' exec --no-startup-id i3-dmenu-desktop

# Show emoji 
bindsym $mod+y exec --no-startup-id emojenu

bindsym $mod+apostrophe exec --no-startup-id $HOME/bin/dmenu_workspace_jumper

focus_follows_mouse yes

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 50
bindsym $mod+Shift+j move down 50
bindsym $mod+Shift+k move up 50
bindsym $mod+Shift+l move right 50

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+semicolon split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Toggle dunst / notification-listener
bindsym $mod+Ctrl+n exec /home/neel/bin/toggle-notif

# Toggle composite manager
bindsym $mod+Ctrl+t exec killall picom || picom

# Show power menu
bindsym $mod+Ctrl+p exec /home/neel/bin/power_menu

#focus on right output 
bindsym $mod+x focus output right

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+period workspace next_on_output
bindsym $mod+Ctrl+period exec --no-startup-id /home/neel/.i3/open_next

bindsym $mod+comma workspace prev_on_output
bindsym $mod+Ctrl+comma exec --no-startup-id /home/neel/.i3/open_prev

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Shift+comma move container to workspace prev_on_output
bindsym $mod+Ctrl+Shift+comma exec --no-startup-id /home/neel/.i3/move_prev

bindsym $mod+Shift+period move container to workspace next_on_output
bindsym $mod+Ctrl+Shift+period exec --no-startup-id /home/neel/.i3/move_next

bindsym $mod+Shift+x move container to output right; focus output right

# Smart Gaps
smart_gaps on
 
# Smart Borders
smart_borders on
 
# Set inner/outer gaps
gaps inner 5
gaps outer 5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

mode "insert" {
    bindsym $mod+Escape mode "default"
}

bindsym $mod+Menu exec xdotool key Caps_Lock

bindsym $mod+r mode "resize"
bindsym $mod+i mode "insert"
bindsym $mod+Ctrl+Shift+i mode "void"

mode "void" {
    bindsym $mod+Ctrl+Shift+i mode "default"
}

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#       	position bottom
#         status_command i3status
#}

# exec --no-startup-id xrandr --output DP1 --auto --left-of eDP1
exec --no-startup-id volumeicon
exec --no-startup-id dunst 
exec --no-startup-id echo "on" > /tmp/notif_status
exec --no-startup-id /usr/lib/kdeconnectd
exec --no-startup-id kdeconnect-indicator
exec --no-startup-id picom

bindsym $mod+Ctrl+l exec /home/neel/bin/lockscreen
bindsym XF86Tools exec /home/neel/bin/lockscreen

#Bind Display Brightness
bindsym XF86MonBrightnessUp exec xbacklight +5
bindsym XF86MonBrightnessDown exec xbacklight -5

bindsym Print exec --no-startup-id ~/bin/capscreen
bindsym $mod+p exec --no-startup-id ~/bin/capscreen

bindsym XF86Calculator exec xterm -e "bc"
# bindsym XF86Explorer exec pcmanfm

for_window [class="(?i)mpv"] floating enable
for_window [class="(?i)gpick"] floating enable
for_window [class="(?i)kcolorchooser"] floating enable
for_window [class="(?i)persepolis"] floating enable
for_window [class="(?i)tlauncher"] floating enable
for_window [class="^.*"] border pixel 3

#######
#THEME#
#######

# set primary gruvbox colorscheme colors
set_from_resource $black color0
set_from_resource $red color1
set_from_resource $green color2
set_from_resource $yellow color3
set_from_resource $blue color4
set_from_resource $magenta color5
set_from_resource $cyan color6
set_from_resource $white color7

set $darkcyan #72dec2

# font used by i3 for titles and bars
font pango:Droid Sans Book 10

# blue gruvbox
# class                 border|backgr|text|indicator|child_border
client.focused          $cyan $green $black $blue $darkcyan
client.focused_inactive $black $black $green $black $black
client.unfocused        $black $black $green $black $black
client.urgent           $red $red $white $red $green

#end of window title bars & borders section

bindsym $mod+t exec birdtray -t
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id xrdb ~/.Xresources
exec --no-startup-id copyq

bindsym $mod+End exec --no-startup-id $HOME/bin/prochunter9x

assign [class="Pulseeffects"] 0
exec --no-startup-id pulseeffects
